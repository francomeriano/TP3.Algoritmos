# suma y resta (+ y -)
suma = 1 + 1
# mult y division (* y /). La division devuelve un float
# potenciacion (exponente)(**)
# division baja (//). devuelve un entero redondeado hacia abajo (int)
# resto o modulo (%)
#type(dato) nos devuelve que tipo de dato es
tipo_de_dato = type(suma)
print(tipo_de_dato)

 

